const { getDirection } = require('../service/location')
const locationModel = require('../database/models/location')
const { create } = require('../database/lib/helpers')


module.exports = {
  locationController: (req, res) => {
    
  },
}
